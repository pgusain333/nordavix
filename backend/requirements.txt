fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
sqlalchemy>=2.0.30
alembic>=1.13.0
asyncpg>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
pandas>=2.2.0
openpyxl>=3.1.0
celery>=5.3.0
redis>=5.0.0
anthropic>=0.28.0
boto3>=1.34.0
httpx>=0.27.0
PyJWT>=2.8.0
cryptography>=42.0.0
sentry-sdk[fastapi]>=1.45.0
# PDF generation for Financial Package exports — pyproject.toml had
# this listed but the production Dockerfile installs from requirements.txt,
# so the dependency wasn't actually shipped. Every PDF export was
# failing with ModuleNotFoundError until this line was added.
reportlab>=4.2.0

# PDF text + table extraction for bank-statement uploads. Pure Python
# (no system deps like poppler/tesseract), works on Fly out of the box.
# Used by modules/recons/bank_pdf.py to parse uploaded bank PDFs into
# the same {txn_date, amount, description, bank_ref} shape that the CSV
# parser produces — downstream code (persist + auto-match) doesn't care
# which format the user uploaded.
pdfplumber>=0.11.0
